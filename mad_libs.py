
print("Welcome to random story maker")

i = 0

while (i < 1):
    non = input('Enter noun: ')
    dest = input('Enter place: ')
    act = input('Enter action: ')
    non2 = input('Enter noun (second person):')
    print(f"{non} went to {dest} and {act} {non2}")
    i = int(input("Enter number other than 0 to close the program"))

# Just preesing enter can also exit the program but making an error